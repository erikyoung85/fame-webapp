import { UnwrapAsyncPipe } from './unwrap-async.pipe';

describe('UnwrapAsyncPipe', () => {
  it('create an instance', () => {
    const pipe = new UnwrapAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
